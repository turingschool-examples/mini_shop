<center>
<h2><%= @item.name %></h2>
<img src= <%= @item.image %> height="125" width="125">
<p>Description: <%= @item.description %></p>
<p>Price: <%= number_to_currency(@item.price) %></p>
<p>Selling Merchant: <%= link_to @item.merchant.name, "/merchants/#{@item.merchant.id}" %></p>
<p>Inventory Count: <%= @item.inventory %></p>
<% if @item.active? %>
  <p><font color="green">Item is Active</font></p>
<% else %>
  <p><font color="red">Item is Inactive</font></p>
<% end %>
<p><%= link_to 'Edit Item', "/items/#{@item.id}/edit" %></p>
<p><%= link_to 'Delete Item', "/items/#{@item.id}", method: :delete %></p>
<% if @item.active? %>
  <p><%= link_to 'Deactivate Item', "/items/#{@item.id}/deactivate", method: :patch %></p>
<% else %>
  <p><%= link_to 'Activate Item', "/items/#{@item.id}/activate", method: :patch %></p>
<% end %>
</center>
