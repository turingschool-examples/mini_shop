<h1>Item Index</h1>

<% @items.each do |item| %>
  <%= link_to item.name, item_path(item) %><br>
  <%= item.merchant.name %><br>
  <%= item.description %><br>
  <%= item.image %><br>
  <%= item.price %>
  <%= item.status %><br>
  <%= item.inventory %><br>

<% end %>
