<h1>Item Index</h1>

<% @items.each do |item| %>
  <%= item.name %><br>
  <%= item.merchant.name %><br>
  <%= item.description %><br>
  <%= item.image %><br>
  <%= item.price %>
  <%= item.status %><br>
  <%= item.inventory %><br>

<% end %>
