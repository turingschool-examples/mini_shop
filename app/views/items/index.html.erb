<h2>All Items</h2>

<% @items.each do |item| %>
  <%= image_tag("#{item.image}") %>
  <p><%= item.name %></p>
  <p><%= item.description %></p>
  <p>$<%= item.price %></p>
  <p><%= item.status %></p>
  <p><%= item.inventory %></p>
  <p><%= item.merchant.name %></p>  
<% end %>
