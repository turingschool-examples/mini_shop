<% @items.each do |item| %>
  <%= link_to item.name, item_path(item) %><br>
  <%= item.price %>
  <%= item.image %>
  <%= item.status %>
  <%= item.inventory %>
<% end %>

<%= link_to "Add Item", new_item_path(@merchant) %><br>

<%= link_to "Merchants", merchants_path%>
<%= link_to "Items", items_path%>
<%= link_to "Home", items_path%>
